# Project Notes

## Terminal Commands for User (CRITICAL)
- User is on **Windows PowerShell** — NEVER use `&&` to chain commands. Always use semicolons (`;`)
- ALWAYS include the full path `cd C:\Users\speer\Dspeer25\lifesaver` before any command
- The dev server (npm run dev) takes over the terminal. User MUST press **Ctrl+C** first to stop it before running any new commands
- Always remind user to press Ctrl+C first if the server is running
